# Agent Local Context: Trin

## Recent Decisions
- **Sprint 4 Phase 5 UAT (2026-07-15, scg monitor Resource Panel STORY-1005, last Sprint 4 phase):** confirmed no parallel/duplicated scan_cache.json readers exist anywhere (grepped). Found the same class of gap I closed in Sprint 2 (mask-event feed only asserting row counts, not content) recurring here — Neo's 4 new panel tests all check `row_count` but never actual cell values. Added one test asserting the real identity/label/timestamp render correctly, not just "a row exists." Worth naming as a recurring pattern at sprint retro (this is the second time this exact gap shape has shown up). PASS, no fix round needed.
- **Sprint 4 Phase 4 UAT (2026-07-15, scg onboard Re-scope & Fatal Exit STORY-1004):** used the real installed binaries directly (`.venv/bin/scalene-guard`/`scg` as actual subprocesses) for both the fatal-exit verification and the onboard-suggestion re-close, rather than testing at the Python-function level — this is the strongest verification available and matches the standing "verify, don't assume" pattern reconfirmed all sprint. Confirmed exit code 2 + plain-language stderr + empty stdout for real cache corruption; confirmed the ordinary case stays exit 0 for contrast. Independently checked (grepped, didn't trust the comment) that `ScannerMachineryError` is genuinely unreachable via a real `scalene-guard` invocation today — `identify()` never calls `.scan()` anywhere. Ran the full onboard-suggestion loop end-to-end through the real binaries: masked call → real suggested command → run it for real → identical call now genuinely allowed. PASS, no fix round needed.
- **Sprint 4 Phase 3 UAT (2026-07-14, Hook Integration STORY-1002/1003):** largest/riskiest phase so far — PASS, but with a real, independently-confirmed regression to carry to Smith's gate. Didn't just trust Neo's `@unittest.skip` reason text on `test_onboard_suggestion_e2e.py`: bypassed the decorator and ran the test body directly, reproducing the exact failure myself (suggested `scg onboard` command writes a rule that has zero effect — the identical call is still masked afterward, since `resource_verifier` reads the scan cache but `scg onboard` still only writes `scalene_policy.yaml`). Also independently verified the first-sighting message wording live (two real calls, one never-seen, one cached-and-verified-untrusted) rather than just reading the branch logic, and independently re-ran both halves of the newly-split perf NFR. This phase is exactly the kind Smith's gate exists for (real user-visible behavior/copy change) — made sure my handoff to Morpheus carries the regression forward explicitly rather than letting a routine "Phase 3 done" summary bury it before it reaches her.
- **Sprint 4 Phase 2 UAT (2026-07-14, Scan Cache Store STORY-1003):** found a real coverage gap in Neo's own test suite (not a code bug) — his dedup test called `try_reserve()` 5x sequentially within one process, which `FileLock` trivially serializes and doesn't exercise the scenario that actually matters: two separate `scalene-guard` invocations (separate OS processes, no shared interpreter state) racing on the same never-cached resource simultaneously. Closed the gap myself with a real `ProcessPoolExecutor`-based test (8 genuine OS processes hammering `try_reserve()` on one resource) — passed cleanly, and re-ran it 5x in a row specifically to rule out a lucky pass on what could have been a real race (a single green run on a race-condition test proves much less than a repeated one). Also independently checked the "no orphaned processes" claim with a real `ps | grep defunct` after running the repeated-invocation test, rather than trusting Neo's `wait()`-based test assertion alone — and independently confirmed the `.gitignore` claim with a real `git check-ignore -v` rather than trusting "I read line 199." PASS, no fix round needed this phase.
- **Sprint 4 Phase 1 UAT (2026-07-14, Scanner Protocol STORY-1001/1002):** found a real, non-edge-case bug by actually executing the code rather than just reading it — `FileScanner`'s path-fallback regex (`\.{0,2}/[\w./+-]+`) had no exclusion for URL slashes, so `https://host/path` matched as a bogus file path on **every** `WebFetch` call (not a rare Bash-command corner case). Caught it by running `FileScanner().identify("WebFetch", {"url": "https://internal.example.com/reports", ...})` directly and getting back a spurious `Resource`, before running the test suite at all. Sent to Neo for the fix rather than patching it myself (kept QA/implementation separate). Independently re-verified post-fix with the original repro *and* a mixed real-path+URL case to confirm the fix (URL-span exclusion, not a lookbehind) didn't over-correct. **Lesson reconfirmed**: a regex that looks conservative/correct on paper (comment claimed "so ordinary words... don't produce false positives") can still have a real gap that only shows up by actually running it against a realistic input shape (a URL, not just plain prose) — code-reading alone likely would have missed this one since the regex's *intent* was right, just its *coverage* wasn't.
- **Sprint 3 Phase 2 UAT (2026-07-14, User Guide STORY-902):** confirmed Smith's Gate 1 note (onboard-suggestion prominence) actually landed by reading the doc, not just trusting Neo's handoff prose. Deliberately did NOT hand-verify Neo's fail-safe bug fix via a manual bash repro — `make test` (137/137, incl. the new regression test) is the check, per this session's user correction against redundant ad hoc verification. PASS. `make judge-trace`: 25 flags cumulative (up from 11 after Phase 1), mostly my/Neo's own `AP-MAKE-PIPE` habit — not blocking, but flagged as a real trend to watch.
- **Sprint 3 Phase 1 UAT (2026-07-14, Getting Started Guide STORY-901):** did the strongest available check for a "clean clone" claim — actually `git clone`d the real repo into a fresh directory and ran `make setup` there (not the already-set-up dev tree), then ran the doc's 3 commands against that clone's own binary. PASS. Also ran `make judge-trace` per the standing UAT rule: found 11 real (not false-positive) process flags from my own verification commands this session (raw `.venv/bin/` calls, piped `make` output, one grep-instead-of-via) — noted in the handoff, not blocking, all self-inflicted during ad hoc checking rather than code/doc defects.
- **Sprint 2 Phase 3 UAT (2026-07-10, Guided Onboarding Action STORY-702):** independently re-ran `make test` (118/118). Independently verified the phase's highest-stakes claim myself, through the *real UI* (not just Neo's isolated-function-level test): drove the actual `MonitorApp` via Pilot keypresses with a real fake-secret-containing target file, confirmed onboarding was genuinely blocked (`"Onboarding blocked: secrets check failed — Possible AWS Access Key detected"`) and **no policy file was written** — the console cannot bypass `onboard.py`'s real safety gate, verified end-to-end through the UI Neo built, not just the underlying function. All 4 of STORY-702's AC bullets checked against real execution, not just code reading.
- **Found one minor doc staleness** (same class as Phase 2's timestamp finding): STORY-702's AC bullet (line 147, `docs/USER_STORIES.md`) still literally quotes the *old* placeholder text `<domain-or-file-this-call-reaches>`, which task 3.1 correctly changed to `<domain-this-call-reaches>` in the actual code. Doesn't change the AC's substance ("editable inline before running" still holds) — flagged to Cypher as a wording cleanup, not blocking.
- **Sprint 2 Phase 2 UAT (2026-07-10, Console Foundations STORY-701):** independently re-ran `make test` (started 107/107, added 1 test myself, ended 108/108). Independently verified (real `pip uninstall textual`, not just a code read) that `scalene onboard` still works and `scalene monitor` gracefully degrades with a plain-language install hint when the `[monitor]` extra is genuinely absent — then reinstalled and re-confirmed the full suite. Confirmed both new Pilot-based tests I touched (the ordering test I added, and re-checking Neo's filter test) are meaningful, not tautological, by breaking each behavior in turn and watching the right test fail, then reverting.
- **Found a real test-coverage gap and closed it myself** (per my own SDET remit — "when the SWE adds a feature, you write the verification tests"): none of Neo's `test_monitor_app.py` tests actually asserted the stated "newest first" ordering AC, only row *counts*. Added `test_events_are_shown_newest_first`, confirmed it fails when the ordering is broken (`reversed(visible)` → `visible`) and passes with the real implementation.
- **Found a genuine spec-authoring conflict, not an implementation defect**: `docs/USER_STORIES.md` STORY-701's AC (line 138) asks the feed to show "tool name, payload field, **timestamp**", but `.scalene/audit.log` entries (`hook_adapter.py`) have never had a timestamp field — confirmed via grep, not assumption. A companion AC (line 140) explicitly forbids changing the audit log format. These two bullets can't both be satisfied literally. Neo's implementation correctly satisfies the *ordering* intent (append-order = chronological) and correctly didn't invent a fake timestamp or touch `hook_adapter.py`'s schema — but silently omitted a timestamp column without flagging the conflict. Not blocking Phase 2 (functional, ordering-correct, no format change) — flagging to Cypher to amend the AC wording (drop "timestamp" or clarify it means ordering, not a displayed clock time) rather than unilaterally rewriting her story text myself.
- **Sprint 2 Phase 1 UAT (2026-07-10, Secrets Scan Upgrade STORY-801):** independently re-ran `make test` (90/90, didn't trust Neo's stated count), read the actual `secrets_scan.py` diff, and ran the real `scalene onboard` CLI end-to-end twice — once against a real file containing a fake AWS key (correctly blocked: `"Onboarding blocked: secrets check failed — Possible AWS Access Key detected"`, plain-language per architecture §11.5) and once against a clean file (correctly succeeded, rule + diff printed). Also independently confirmed no leftover temp files after scanning (`tempfile.mkstemp`'s cleanup is real, not just claimed) and no stray `scalene_policy.yaml` left in the repo after my manual CLI runs.
- Independently verified Neo's "no network egress" claim myself rather than trusting the regression test alone — read the actual `test_never_attempts_network_access` test (patches `socket.socket.connect` to raise, confirms scan still succeeds), and separately confirmed by reasoning through the code path (`tempfile`/`os.fdopen`/`scan_file` — none of these touch sockets) that the claim holds.
- **Minor non-blocking observation:** Neo's edit added quotes around the AWS-key test fixture (`key = {fake_key}` → `key = "{fake_key}"`), but I independently tested both forms — `AWSKeyDetector` catches the unquoted form too (it's a fixed-pattern regex detector, not one of the quote-requiring `HighEntropyStringsPlugin` subclasses). The quoting wasn't strictly necessary, but it doesn't weaken the test or violate the "fix fixtures, don't add scanner exceptions" policy — just a harmless stylistic match to the newer bare-token test. Not filing as a bug, noting for completeness.
- Phase 1 UAT (2026-07-08): cross-checked `src/scalene/taint_state.py` + `policy_config.py` against every AC bullet in `docs/USER_STORIES.md` STORY-101, 102, 201, 202. All satisfied for what's in-scope for Phase 1; STORY-102's "runs on every tool result via post_tool_call" bullet is correctly deferred to Phase 2 (STORY-302, PostToolUse adapter) per `task.md`'s phase mapping — Phase 1 only needed to deliver the `evaluate()` primitive.
- Added `test_fail_safe_path_is_logged_not_silent` (uses `assertLogs`) because the existing tests only checked the fail-safe *side effect* (`fail_safe_triggered=True`), not the AC's explicit "logged (not silent)" requirement.
- Phase 2 UAT (2026-07-09): cross-checked `masking.py` + `hook_adapter.py` against STORY-301, 302, 401. Ran an informal timing sanity check on `pre_tool_use` (~6ms/call avg over 200 calls) — comfortably under the 15ms NFR, but did **not** treat this as a formal pass of STORY-301's perf AC bullet: `task.md` Phase 4 Task 4.2 explicitly owns rigorous perf verification, so that AC stays open until then.
- Phase 3 UAT (2026-07-09): cross-checked `reputation.py`/`secrets_scan.py`/`subprocess_isolation.py`/`onboard.py` against STORY-501, 601. Did **not** just trust Neo's "no network calls" claim — independently re-ran the grep myself (artifact-based verification protocol: don't guess/trust, check). Also cross-checked STORY-501's "attributable (git-committed)" AC against `ARCHITECTURE.md` §8 directly: architecture explicitly resolves this as diff+audit-log output (no auto-commit), which is what Neo built — not a gap.
- Phase 4 UAT (2026-07-09, final sprint UAT): found a real bug in Neo's `docs/STORY_TRACEABILITY.md` — its summary line claimed "33 of 35 AC bullets," but an independent `grep -c "^\- \[ \]"` against `docs/USER_STORIES.md` gives 31 total, and the table itself has exactly 31 content rows (29 test-verified + 2 design-verified). Fixed the summary line in place rather than just filing it as a finding, since it was a one-line, unambiguous correction. This is exactly why artifact-based verification means independently counting, not trusting a stated number.

## Key Findings (2026-07-09, `*qa uat copyable system messages`)
- Lesson for future UAT of any "here's a command to copy-paste" feature: don't just check the string's *content* is correct — actually paste it into a real shell. `shlex.quote()` applied to every argument except one (a hardcoded placeholder) is exactly the kind of asymmetry that's invisible in a code read but breaks immediately on first real use. Artifact-based verification protocol extends to "run the exact thing a user would run," not just "read the code that builds it."


- `PolicyConfig.evaluate()`'s fail-safe trigger is scoped to exceptions during JSONPath parse/find (malformed expression or incompatible data shape) — not "no rule matched," which is normal default-driven behavior. This matches architecture §2's "any ambiguity resolves to sensitive=true, trusted=false," not every non-match.
- STORY-401's "never throws a runtime error" AC is tested at both layers: `MaskingEngine.apply_mask` directly (missing field/None field/non-dict args) and `hook_adapter.pre_tool_use` end-to-end (unmapped tool name) — good defense in depth, not redundant.
- STORY-601's isolation is tested at two levels too: a mocked `subprocess.run` call (asserts `SCALENE_BYPASS=1` is actually in the env kwarg and that `scalene.scan_worker` is the module invoked) plus real, non-mocked end-to-end runs (`test_subprocess_isolation.py`'s secrets/reputation tests actually spawn the subprocess).
- The 2 design-verified-only AC bullets (STORY-301 cross-platform NFR-Portability, STORY-302 sanitized-output-is-ordering-not-mechanism) are legitimate to accept at sprint close — no CI/Docker test matrix exists in this repo to test the former directly, and the latter isn't a separate mechanism to test.

## Important Notes
- Verdict: **Phase 1 PASSED** (20/20). **Phase 2 PASSED** (40/40). **Phase 3 PASSED** (67/67). **Phase 4 / full sprint UAT PASSED** (77/77, no regressions) — after fixing the traceability-doc count bug. All 29 test-verifiable AC bullets across all 9 stories pass; 2 are accepted as design-verified. Handed to Morpheus for final architecture/quality review and sprint close.
- `*qa judge session` (2026-07-09): compiled and verified the judge loop for today's full-session tool/skill usage. TES 98/100 (Smith). 3 findings (S1-001 double `Skill(make)`, S1-002 `git stash -u` care lapse, S1-003 Smith's UX gate never ran) all addressed by Bob — 2 in `agents/skills/bloop/SKILL.md` (concrete anchor for rule #6; new conditional `3b` Smith UX-gate step in the `*impl` chain), 1 as a cross-session memory (git-stash safety, not project-specific). Verified: edits render correctly, `make test` still 77/77 (doc-only changes). Loop closed — no further iteration needed.

---
*Last updated: 2026-07-09*
