# Agent Local Context: Morpheus

## Recent Decisions
- **Sprint 4 Phase 2 review (2026-07-14) — APPROVED, with a major escalated finding:** personally measured the "zero-added-latency" claim I wrote into §13.3 for the new-resource cache path and found it's real but false as currently implemented — `refresh_if_needed()`'s `subprocess.Popen` spawn (fire-and-forget) costs ~6.6ms avg / ~16ms max per call in this environment, isolated to the spawn syscall itself (reproduced identically with a trivial no-op command, not the worker script's complexity). Against a <15ms total hot-path NFR, this is a real risk, not a theoretical one — a single first-sighting call could burn most/all of the budget, and multiple never-seen resources in one call multiply it. Also found (non-blocking, self-healing) that `cache_refresh_worker.py`'s exception handling doesn't cover `ScanCache.put()`, only `scanner.scan()` — bounded impact since it's a detached worker, doesn't affect any live decision, just delays that one resource's next retry by up to 5 minutes. Approved Phase 2 on its own narrow scope (it's functionally correct, well-tested, matches spec) but paused before Phase 3 to put real numbers behind Smith's Gate 2 watch-item / task 3.4's perf-re-verification requirement, since Phase 3 needs an actual design decision (not just "re-run the perf test and hope") now that the underlying assumption is confirmed broken.
- **Sprint 4 Phase 1 review (2026-07-14) — APPROVED:** `src/scalene/scanner.py` matches §13.2 literally (types, registry shape, extensibility). Personally adversarial-tested `identify()`/`scan()` before approving (mixed non-string arg types, empty args, empty known-field values — all clean). Reviewed Trin's URL/path-collision finding + Neo's fix and confirmed the fix generalizes (span-exclusion, not a one-string patch). One design point worth remembering: `Scanner.scan()` implementations never raise today (any `run_scanner` failure — finding or machinery breakage — becomes a `ScanResult` label), which is fine for Phase 1 but means Phase 4's fatal-exit work (STORY-1004) can't rely on "scanner raises" as its trigger unless `run_scanner`'s `{"ok", "reason"}` contract gets a real distinguishing signal added, or Phase 4 leans entirely on the cache-store-corruption trigger instead. Already flagged in Neo's own `next_steps.md` — noting here too so it isn't lost between now and Phase 4.
- **Sprint 4 architecture (2026-07-14), `docs/ARCHITECTURE.md` §13 (E10 — Extensible Scanner Registry & Resource Verification):** decided full replacement of the just-shipped `PolicyConfig.allowlist`/`PolicyRule` model (not coexistence) — the vouch-forever-on-a-broad-pattern defect is structural, keeping the old path anywhere leaves it open. Designed `Scanner` protocol (`identify()`/`scan()`) + registry, a `.scalene/scan_cache.json` store (`filelock`-protected) with 3-state (none/fresh/expired) lookup where the "new resource" path is provably zero-latency (identical to today's fail-safe default, just adds a background seed-the-cache side effect). Background = detached `Popen`, no daemon. Bash reuses File/URL scanners' fallback detection rather than getting its own scanner type. `scg onboard` re-scoped to pre-seed the cache instead of writing a rule. Fatal-exit boundary specified precisely (scanning machinery failure only, never a scan finding) with the exact exit code explicitly left for Neo to verify against the real Claude Code contract, not assumed. Documented exactly how this integrates with (and doesn't change) `MaskingEngine.decide()`'s existing content-gating check — two separate mechanisms, only one is being replaced.
- **Sprint 3 plan review (2026-07-14):** reviewed Mouse's `task.md` Sprint 3 phase breakdown against my own §12 architecture. **APPROVED**, no adjustment needed — phase sequencing matches §12 exactly (Getting Started → User Guide → Demo, no hard dependencies), Smith gate placement matches my expectation (required on Phase 1 for the timed AC and Phase 3 for the new demo surface, correctly omitted on Phase 2's pure-accuracy-check), both my Gate-1/Gate-2 notes (onboard-suggestion prominence, BRD-naive narration) are explicitly folded into named tasks (2.3, 3.1) rather than left as loose prose. No Tank phase confirmed correct. **Sprint 3 plan LOCKED.** Handed Phase 1 to Neo.
- **Sprint 3 architecture (2026-07-14), `docs/ARCHITECTURE.md` §12 (E9 — Documentation & Onboarding):** STORY-901/902 are placement-only (new `docs/` files, README table update, no code). STORY-903 (demo): decided the demo runs the *real* `scalene-guard` CLI as a subprocess fed real hook JSON on stdin (not internal function calls), scenario = a `Read` of a fake sensitive file followed by a `WebFetch` to an untrusted domain, showing the actual masked response. Stays offline for free — `scalene-guard` never performs the tool call itself, it's decision-only (§1), so showing the masked `tool_input` *is* the demo; no mocking needed. Also decided the demo is asserted against in `tests/test_demo.py` (subprocess + output assertions) so it can't rot silently, with a `make demo` target for human viewing. No Tank gate — local dev script only, same precedent as Sprints 1-2.
- **Sprint 2 Phase 3 review (2026-07-10, Guided Onboarding Action) — REJECTED, round 1:** ran `apply_onboard_command()` myself with two deliberately adversarial inputs before approving anything, rather than trusting Neo's/Trin's happy-path verification. Found 2 real, user-triggerable uncaught exceptions: `FileNotFoundError` if `scalene` isn't resolvable on PATH (plausible on a fresh install before a shell restart), and `ValueError` from `shlex.split` on an unbalanced quote (an easy typo while hand-editing a target path/domain inline — exactly the editing this phase's AC asks for). Either would crash the entire TUI, not just fail the one action. Every other error boundary in this codebase (`onboard.py`'s `OnboardError`, `scan_worker.py`'s `{"ok": False, "reason": ...}`, `hook_adapter.py` never raising to the agent) returns a plain-language result instead of propagating an exception — this is the one place that doesn't, on the phase explicitly documented as the sprint's highest-stakes surface. Sent back to Neo.
- **Sprint 2 Phase 2 review (2026-07-10, Console Foundations):** APPROVED. Independently re-grepped `hook_adapter.py`/`cli.py` myself to confirm the `[monitor]` extra's isolation claim holds (matches my own §11.1 decision). 2 non-blocking notes: (1) `MonitorApp._events` grows unbounded for the life of a running monitor session — acceptable for this phase's "console foundations" scope (the audit log itself is equally unbounded/unrotated already), revisit if long-running/high-volume use becomes an actual complaint; (2) `refresh_sessions()` does a full `table.clear()` + repopulate on every 500ms poll tick regardless of whether anything changed — flagged to Smith specifically, since whether this causes visible cursor/selection flicker in a real terminal is a UX-observable question only her `*user test` can answer, not something to judge from a code read.
- **Sprint 2 architecture (2026-07-10), `docs/ARCHITECTURE.md` §11:** Resolved Cypher's TUI-vs-web open question in favor of a **TUI (`textual`)**, packaged as an optional pip extra so the hot-path hook adapter never gains a TUI dependency. Reason: a web frontend needs a bound localhost port + web-server dependency (project has zero today) — exactly the kind of thing that would trigger a Tank gate; a TUI reads local files directly and stays in the terminal the developer already has open. **Consequence: no Tank gate this sprint** (no new port/service/env var/CI impact), same precedent as Sprint 1.
- **Data access: poll, don't watch** — `.scalene/audit.log`/state files read via fixed-interval polling, not `watchdog`/inotify. Reason: NFR-Portability (inotify is flaky on some Docker bind-mounts/network FS), avoids a new dependency for a dev-convenience tool, files are small so poll cost is negligible.
- **Session scope (resolves Smith's Gate 1 note on STORY-701):** default view lists all sessions with a discoverable state file, selectable to filter the mask feed; an "all sessions" aggregate toggle also exists. Fed back to Cypher as an explicit AC to add, same pattern as Sprint 1's §8.
- **Onboarding action (STORY-702):** console's "apply" shells out to the existing `scalene onboard` CLI using the real `suggested_onboard_command` string — UI shell only, never a reimplementation, so it can't bypass the existing scan/reputation gates.
- **Secrets scan error messages (resolves Smith's Gate 1 note on STORY-801):** `detect-secrets` output must be translated through `secrets_scan.py`'s existing plain-language result format, never surfaced as raw library exception text. Fed back to Cypher as an explicit AC to add.
- **Two-layer architecture**: Hook Adapter (Claude Code-specific) + Policy Engine (harness-agnostic). Reason: BRD calls for cross-platform portability and the BRD's own YAML example uses generic tool names (`view_file`, `read_file`) rather than Claude-Code-specific ones — treating harness coupling as an adapter concern keeps a future Cursor/other-harness port from being a rewrite.
- **No daemon** — state externalized to per-session JSON files (`.scalene/state/<session_id>.json`), loaded/saved per hook subprocess invocation. Reason: NFR-Portability (must run in Docker/cloud dev envs without assuming a long-lived process is allowed) and simplicity — avoids crash-recovery/IPC design entirely.
- **Threat-intel for trust-list onboarding (STORY-501): rejected external paid API for v1.** Shipping a `LocalHeuristicChecker` (domain heuristics, no network call) behind a `ReputationChecker` interface instead. This directly resolves the Tank-flag concern Cypher raised (no new external dependency/egress in v1) — pluggable interface means a real API can be added later without touching callers.
- **Python 3.11+ / pip-installable CLI** as the stack. Reason: matches existing tooling conventions already in this repo (`via` is pip-installed).
- **Approved Mouse's task.md as-is**, with one addendum: folded a `.gitignore`/CI-artifact check for `.scalene/` into task 4.1 rather than spinning up a full Tank phase for it — Mouse's no-Tank-phase call was right (no deploy/CI/infra work this sprint), this is just a one-line hygiene check, not worth a phase.

## Key Findings (2026-07-09, `*lead review copyable system messages`)
- `_suggest_onboard_command()` (`hook_adapter.py`) is harness-agnostic (takes primitives, never touches Claude Code's hook JSON shape) but lives in the adapter layer, which §2's "adapter isolation" principle reserves for harness-specific translation. Not blocking — private helper, cheap to relocate — but flagged so it isn't forgotten if/when a second harness adapter is actually built: move it to the policy-engine layer (`masking.py` or a new small module) at that point so both adapters can share it instead of duplicating.
- This is the first concrete instance of the §10 technical-debt watch item ("the adapter/policy-engine seam is the one most likely to be tested by a second-harness port") actually surfacing — worth remembering as a precedent, not just a one-off.


- The BRD's hook naming (`pre_tool_call_hook`, `post_tool_call_hook`) maps directly onto Claude Code's actual `PreToolUse`/`PostToolUse` hook mechanism — the same one active in this very session. Chose to target it explicitly rather than inventing a generic hook protocol nobody would consume yet.
- Addressed both of Smith's Gate 1 notes architecturally rather than leaving them purely as story AC: masking events get an audit log entry AND a `systemMessage` surfaced into the transcript (visibility); onboarding CLI prints a confirmation + diff (STORY-501 success feedback).
- task.md's phase sequencing matches the architecture's dependency graph correctly: Foundations (config+state) before both Hook Adapter (needs both) and Onboarding (needs config only) — Onboarding could theoretically run parallel to Phase 2 but Mouse kept it sequential for a single-implementer sprint, which is the right call here.

## Important Notes
- Sprint plan is now **locked**. `docs/PRD.md`, `docs/USER_STORIES.md`, `docs/ARCHITECTURE.md`, and `task.md` are all in place. The `*plan sprint` Bloop chain (Cypher → Smith Gate1 → Morpheus → Smith Gate2 → Mouse → Morpheus review) is complete.
- Next actual work is `*impl phase-1` (Neo) whenever the user is ready — not part of this planning chain.

---
*Last updated: 2026-07-14*
