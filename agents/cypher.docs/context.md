# Agent Local Context: Cypher

## Recent Decisions
- **Sprint 10 planning (2026-07-22):** wrote E16 (Interactive Onboarding Dashboard & Non-Blocking Review Loop — STORY-1601 through 1606) into `docs/USER_STORIES.md`, updated `docs/PRD.md` epic table + Sprint 10 goals. Origin: user's direct `*user consult` with Smith, extending `scg monitor` (E7/E10 Phase 5) into an interactive Verify/Allow/Deny review surface. Both hard design questions Smith raised in the consult were already resolved by the time I storied this — the hook stays synchronous (STORY-1601 rides the existing `reason` field, no contract change) and "dirty flag" = `ScanCache.is_fresh()`, already shipped, just not displayed (STORY-1602/1604). Read the real code (`hook_adapter.py`, `monitor_app.py`, `monitor_data.py`, `resource_verifier.py`, `scan_cache.py`) before writing ACs. STORY-1606 (read-only-to-agent policy boundary) flagged for Tank, explicitly scoped to what Scalene's own setup can enforce — not third-party agent sandboxes generally, which is out of scope. Full requirements trace lives in the story origin note + `agents/smith.docs/e16_onboarding_tui_consult.md`. Handed to Smith for Gate 1.
- **Sprint 9 planning (2026-07-21):** wrote E15 (Configurable Scanner Registry & Extended Scanner Coverage — STORY-1501 through 1504) into `docs/USER_STORIES.md`, updated `docs/PRD.md` epic table + Sprint 9 goals. Origin: user's own scanner-focused-sprint direction, delivered as two messages (a dictated/voice one, then a follow-up). Read `scanner.py`, `reputation.py`, `scan_cache.py` before writing — confirmed 3 things the user described (identify()/scan() protocol, FileScanner reusing secrets-detection, 24h scan-cache expiration) are already shipped since E10, so did not re-story them; only storied what's genuinely new (config-driven registry, hardcoded restricted-path defaults, real external reputation source, project-folder default posture). Flagged STORY-1504 explicitly as being in tension with PRD Goal 5 (fail-safe defaults) rather than silently treating it as a routine addition — scoped its AC narrowly (project root only, still overridable by STORY-1502) so the tension is visible to Morpheus/Smith at their gates, not papered over. Full requirements capture in `agents/cypher.docs/e15_scanner_sprint_req.md`. Handed to Smith for Gate 1.
- **Sprint 5 planning (2026-07-17):** wrote E11 (Trust/Sensitivity Model & Rule-Driven Resource Identity — STORY-1101 through 1105) into `docs/USER_STORIES.md`, updated `docs/PRD.md` epic table + Sprint 5 goals. Origin: two threads converged — the user's per-resource `mask`/`block` onboarding proposal exposed that `is_trusted` today skips scanning entirely (so a per-resource mode would never fire), and working through that with Morpheus surfaced the bigger issue: `URLScanner`'s host-only identity (shipped in E10) reproduces E10's own "one scan vouches for an unbounded set" defect. Full corrected design already written by Morpheus/user in `docs/ARCHITECTURE.md` §13.8 — my job was formalizing it as testable stories, not designing it. Included STORY-1105 (cache-key migration) explicitly because Morpheus's own `next_steps.md` flagged it as a gap in §13.8 that could otherwise get silently skipped during implementation. Left §13.8's own 4 open questions (default-rule JSONPath, scanner explicit-vs-inferred, real on-disk schema, `scg onboard` mapping) as explicit open questions for Morpheus rather than pre-deciding them.
- **Sprint 4 planning (2026-07-14):** wrote E10 (Extensible Scanner Registry & Resource Verification — STORY-1001 through 1005) into `docs/USER_STORIES.md`, updated `docs/PRD.md`. Origin: a direct user design session found a real hole in the URI-scheme onboarding model shipped just before this (one-time scan of a representative target vouching for an unbounded pattern-matched future set) and specified the fix in full through conversation — my job here was capturing it as testable stories, not inventing requirements. Left the "replace vs. coexist with `scg onboard`" question explicitly open for Morpheus rather than pre-deciding a product-scope question that's really an architecture one.
- **Sprint 3 planning (2026-07-13):** wrote E9 (Documentation & Onboarding — STORY-901/902/903) into `docs/USER_STORIES.md`, updated `docs/PRD.md` epic table + Sprint 3 goals. Scope per direct user request: a `GETTING_STARTED.md` (fast clean-clone walkthrough), a `USER_GUIDE.md` (full CLI/config reference), and a runnable/testable demo of Scalene stopping a real exfiltration attempt. Deliberately scoped each story's AC to require linking to existing docs (README, SETUP.md, ARCHITECTURE.md) instead of forking/duplicating content, since this project already has doc-drift history (Sprint 2 retro: 2 AC-text staleness items found mid-UAT).
- **Sprint 2 planning (2026-07-10):** wrote E7 (Live Taint & List Management Console — STORY-701/702) and E8 (Category-Aware Secrets Scan — STORY-801, promoted from `morpheus.docs/BACKLOG.md`) into `docs/USER_STORIES.md`, updated `docs/PRD.md` epic table + goals. Scope per direct user request: usability focus, include backlogged items, and a new realtime dashboard over the existing `.scalene/audit.log`/taint-state files with one-click onboarding of suggested rules.
- Reconciled this file's own staleness on entry: `current_task.md` still said "Awaiting Smith gate 1 approval" from 2026-07-02, but `agents/CHAT.md:23-32` shows both Sprint 1 gates (`*user approve`) passed that same day, and Sprint 1 fully shipped/closed 2026-07-09 (`task.md` header). The file was simply never updated after gate 1 passed — no content was ever actually blocked. Oracle flagged this staleness on 2026-07-10; fixing it here rather than leaving it for Oracle since it's Cypher's own narrative content.
- Left 2 items explicitly un-storied per Bloop fast-track rule (too small for a full Smith-gated story): the onboard-command placeholder wording fix, and relocating `_suggest_onboard_command()` (deferred until a 2nd harness adapter exists) — both noted in `docs/USER_STORIES.md`'s "Deferred / Not Promoted" section for Mouse to fold in as small tasks.
- Did NOT promote `scrubadub`/`presidio` category-aware *mask-literal* work from the backlog into a story — the backlog note itself says that's only needed "if/when category-aware masking becomes an actual requirement," which it isn't yet. Only the `detect-secrets` onboarding-scan upgrade (STORY-801) was promoted.
- Structured Project Scalene into 6 epics (E1-E6) mapped directly from BRD sections 2.3.1-2.3.4 plus fail-safe/isolation (BRD 2.4/NFR-Security).
- Split masking (E4) as its own epic separate from hooks (E3) since masking is a decision layer, hooks are the interception mechanism.
- Flagged two stories (STORY-501 trust-list onboarding, STORY-601 scanner isolation) for Tank review — both introduce external service calls / env vars / process isolation, which is infra territory per Cypher-Tank relationship rules.

## Key Findings
- BRD is unusually complete for a fresh repo (includes YAML schema example, data flow diagram, NFRs with concrete numbers). Treated it as close to authoritative rather than re-deriving requirements from scratch.
- BRD does not specify: implementation language/runtime, hook registration mechanism (which agent harness API), or where taint state persists. Logged these as open questions for Morpheus rather than guessing.

## Important Notes
- `docs/PRD.md` and `docs/USER_STORIES.md` created — first PRD/stories for this project, no prior versions to reconcile.
- This is sprint-planning Step 1 of the Bloop `*plan sprint` chain. Next gate is Smith (`*user review`) before Morpheus architecture.

---
*Last updated: 2026-07-02*
