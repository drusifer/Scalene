# Agent Local Context: Oracle

## Recent Decisions
- **`*ora groom` (Sprint 10/E16, 2026-07-23)**: reconciled STORY-1601-1605's ACs against real shipped mechanisms, checked off individually with the specific test/verification each one traces to (not force-checked in bulk). One honest reconciliation, same precedent as Sprint 9's: STORY-1604's "visible in-progress indicator per target" as literally worded implied a per-row spinner; what shipped instead reuses STORY-1602's Scanners busy/idle panel as the in-progress signal during Verify (`try_reserve`/`put` around each scan) — satisfies the AC's real intent (operator can see a scan is in flight) via the existing signal, not a silently-skipped requirement. STORY-1606 recorded as shipped-scope-reduced: its original AC (OS-level read-only mount, Tank-verified) was shelved mid-phase by direct user decision, and the replacement design (hash-snapshot detect-and-restore) is fully written in `docs/ARCHITECTURE.md` §20.6 but backlogged, not built — carried forward explicitly, not dropped. Archived `CHAT.md` (469 → 154 live lines, new archive `CHAT-ARCHIVE-20260722.md`, cut at Sprint 9's own pre-context-clear state-save boundary, ~75% per the standing rule). Recorded 2 durable lessons: distinguishing operation cost *class* (subprocess spawn vs. a plain file append) before reusing a previously-measured NFR's caution level for an unrelated operation; a simulated UI click at an off-screen/clipped widget region is a silent no-op, not an error — check terminal/viewport size before assuming an interaction-test failure is application logic. Updated `docs/USER_STORIES.md`'s and `docs/PRD.md`'s status lines to reflect E16 shipped (438/438 tests), not drafted.
- **`*ora groom` (Sprint 9/E15, 2026-07-21)**: reconciled all 4 story ACs against real shipped mechanisms, not force-checked. Two stories (STORY-1502, STORY-1504) had ACs literally worded around vocabulary ("restricted"/"untrusted"/"trust=trusted") that turned out not to correspond to any real system state once Neo traced the actual control flow during implementation — added reconciliation notes explaining the real mechanism (an unconditional block; sensitivity escalation with trust simply never contaminated) satisfies the AC's real intent even though the literal wording predates that clarity, same honest-reconciliation precedent as prior sprints' story grooming. Resolved all 5 of Cypher's carried-forward open questions with brief notes. Archived `CHAT.md` (111 → 47 live messages, new archive `CHAT-ARCHIVE-20260721.md`, natural cut at the session's own state-save boundary rather than an arbitrary line count). Recorded 2 durable lessons: architecture claims about *existing* code need the same verify-don't-assume standard as claims about new code (2 real corrections this sprint alone); a broad default rule written first can silently shadow a more specific rule written later under an unrelated append-only convention — a new-feature-interacts-with-old-mechanism bug shape, not a new-code-in-isolation bug. Also fixed `docs/PRD.md`'s top status line, stale since Sprint 4 while `USER_STORIES.md`'s own status line stayed current every sprint — pointed it at the latter instead of letting both drift independently.
- `*ora groom docs` (2026-07-10): found one new orphan created *after* the 07-09 pass — `url-malware-checking.md` at root, landed in the `8d77f9e` "copyable messages" commit alongside unrelated code changes (so the prior groom pass, done earlier the same day, never saw it). Zero references anywhere (grepped `.md`/`.py`/`CHAT.md` — none). `git mv`'d to `docs/URL_MALWARE_CHECKING.md` to match the project's `ALL_CAPS.md` docs-folder convention, and added it to `README.md`'s doc table. Skipped `make test` — pure doc move, no code/test referenced the old path (verified via grep before moving, not after).
- `*ora groom docs` (2026-07-09): moved the one orphan root file, `Scalene BRD.md` (space in filename, no docs/ home) → `docs/BRD.md` via `git mv` to preserve history. Updated the two live references to it (`docs/PRD.md`'s Source line, `agents/cypher.docs/current_task.md`) — deliberately left `agents/CHAT.md`'s reference untouched since it's an append-only historical log, not a live doc.
- Rewrote root `README.md`: was 2 lines with no links. Added a doc table (BRD/PRD/USER_STORIES/ARCHITECTURE/SETUP/STORY_TRACEABILITY/task.md), a `make setup && make test` quickstart, and a pointer to `agents/DOCUMENTATION_INDEX.md` for the Bob Protocol coordination layer.
- Fixed `agents/DOCUMENTATION_INDEX.md`: it referenced 4 files that don't exist in this project (`STARTUP.md`, `SHORTHAND_GUIDE.md`, `BOB_SYSTEM_PROTOCOL.md`, `START_HERE.md`) and one nonexistent archive file (`.archive_state_management_fix.md`) — these read like generic bob-protocol template boilerplate that was never actually created here. Removed the dead links rather than leaving them dangling. Also added a new "Project Documentation (Scalene)" section since the index previously only covered framework meta-docs, not the actual project docs in `docs/`.
- Verified every link I touched or added actually resolves (checked file existence for each relative path from its source file's directory) before finishing — did not assume paths were correct.

## Key Findings
- `agents/cypher.docs/current_task.md` is significantly stale (still says "Awaiting Smith gate 1 approval" as of 2026-07-02, but Sprint 1 fully closed 2026-07-09 per `agents/CHAT.md` and `task.md`). Did **not** rewrite Cypher's narrative/status content myself — that's outside Oracle's remit per the "don't overwrite persona content without permission" rule; only fixed the one mechanical broken-link reference inside it. Flagging here so Cypher's next activation (or the user) knows to reconcile it.
- `task.md` and the symlinked root files (`AGENTS.md`, `CHATGPT.md`, `GEMINI.md`, `.cursorrules` → `agents/AGENTS.md`) are intentional per bob-protocol convention, not orphans — left alone.

## Important Notes
- Grooming pass scope: root-level orphan files + broken links in `agents/DOCUMENTATION_INDEX.md` + `README.md` ToC. Did not touch `docs/ARCHITECTURE.md`/`PRD.md`/`USER_STORIES.md` content itself (those are Morpheus's/Cypher's owned documents, structurally fine, not orphaned).
- Ran `make test` after the file move (77/77 still passing) to confirm no code referenced the old `Scalene BRD.md` path.

---
*Last updated: 2026-07-09*
